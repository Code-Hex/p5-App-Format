use strict;
  use warnings;

      my @abc = ();

sub aaa {
    my $a = shift;
print $a;
    @abc = (
        1,
        2,
        3
    );
}

sub bbb {
    my $b = shift;
    my $ccc = sub {
        my $x = shift;
print "Hello$x";
  print "world";
  my $ref = "ref";
  my $hash = {
      a => "Hello",
      bcd => "World",
      "bdss_sdd" => 100,
     };

  my $hash2 = +{
      a => sub {
          print "x";
    },
         $ref => [
             "Hello",
             "world",
         ],
bsx => -10.10,
      bsscd => -10,
      100 => 100,
     };
};
}

  print "Hello"x10;
print "End\n";

package Hello::World {
    use strict;
  use warnings;

      my @abc = ();

sub aaa {
    my $a = shift;
print $a;
    @abc = (
        1,
        2,
        3
    );
}

sub bbb {
    my $b = shift;
    my $ccc = sub {
        my $x = shift;
print "Hello$x";
  print "world";
  my $ref = "ref";
  my $hash = {
      a => "Hello",
      bcd => "World",
      "bdss_sdd" => 100,
     };

  my $hash2 = +{
      a => sub {
          print "x";
    },
         $ref => [
             "Hello",
             "world",
         ],
bsx => -10.10,
      bsscd => -10,
      100 => 100,
     };
};
}

  print "Hello"x10;
print "End\n";
};